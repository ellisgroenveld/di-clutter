# di-clutter
A project application about projects

in VSC, set Ghapi token by doing `export GH_TOKEN=YOUR_TOKEN` where `YOUR_TOKEN` is the Github developer portal token you have.
Note that there are probably more secure ways of doing this but I was lazy for now.